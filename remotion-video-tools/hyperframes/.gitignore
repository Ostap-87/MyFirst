# Собранные проекты: в каждом лежит копия исходной съёмки на десятки мегабайт.
# Пересобираются из clips.json одной командой.
batch/

# Библиотеки: three качается по версии из build.mjs, gsap берётся из навыка.
# Чужой код в истории проекта не нужен.
vendor/
